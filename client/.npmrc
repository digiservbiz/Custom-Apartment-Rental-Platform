# react-scripts@5 pins typescript@^3||^4 as an optional peer while newer
# libraries want ^5; this project uses no TypeScript, so skip strict peer
# resolution entirely. Applies to npm install and npm ci (local and CI).
legacy-peer-deps=true
